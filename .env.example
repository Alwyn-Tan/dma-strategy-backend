# ==============================================
# Django 核心配置
# ==============================================
# 项目加密密钥（生产环境需替换为复杂随机字符串）
# 生成方式：python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secure-secret-key-here-eg-8Z7xQr2tK9pW5mN3bV7dF1gH0jL6kP4s

# 调试模式（开发环境=True，生产环境=False）
DEBUG=True

# 允许访问的域名（生产环境替换为你的实际域名，多个用逗号分隔）
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.100

# MVP 数据目录（CSV 数据源）
DATA_DIR=./data


# ==============================================
# PostgreSQL 数据库配置
# ==============================================
# 数据库名称（默认与项目匹配，无需修改）
DB_NAME=stock_db

# 数据库用户名（本地开发默认 postgres，生产环境需用专用账号）
DB_USER=postgres

# 数据库密码（本地开发可简化，生产环境需设置强密码）
DB_PASSWORD=your-db-password-here-eg-Postgres123!

# 数据库地址（本地开发=localhost；Docker 部署=db）
DB_HOST=localhost

# 数据库端口（PostgreSQL 默认 5432，除非手动修改）
DB_PORT=5432


# ==============================================
# Redis 缓存配置
# ==============================================
# Redis 连接地址（本地开发=localhost；Docker 部署=redis）
REDIS_URL=redis://localhost:6379/1


# ==============================================
# 可选配置（按需启用）
# ==============================================
# 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL，默认 INFO）
LOG_LEVEL=INFO

# Celery 消息队列地址（如需使用异步任务，与 Redis 地址一致即可）
# CELERY_BROKER_URL=redis://localhost:6379/2
